<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Amount</title>
    <link rel="stylesheet" href="path/to/your/styles.css">
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        @font-face {
            font-family: Chap;
            src: url("../fonts/Chap-Regular/Chap-Regular.ttf");
        }
        html {
            font-family: Chap, sans-serif;
            background-color: #f4f4f4;
        }

        .container {
            max-width: 500px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="number"],
        input[type="textarea"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        .button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>Edit Amount</h1>
    <form action="/edit-amount/<%=debtorId.id%>?_method=PUT" method="POST" class="edit-form">
        <div class="form-group">
            <label for="debtorName">Debtor Name:</label>
            <input type="text" id="debt" name="debt" value="<%= debtorId.debt %>" readonly>
        </div>
        <div class="form-group">
            <label for="newAmount">New Amount:</label>
            <input type="number" id="amount" name="amount" placeholder="Enter new amount" value="<%= debtorId.amount %>" required>
        </div>
        <div class="form-group">
            <label for="desc">Products Loaned:</label>
            <input type="textarea" id="desc" name="desc" placeholder="Enter products loaned" value="<%= debtorId.desc %>" required>
        </div>
        <button type="submit" class="button">Update</button>
    </form>
</div>
</body>
</html>
